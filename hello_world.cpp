#include <iostream>
//This program greets the user by name
int main() {
    std::string name;
    std::count << "Enter your name: "; //Ask user for their name
    std::cin  >> name; //Read user's name
    std::count << "Hello world from " << name << endl; // Greet the user
    return 0;
}

